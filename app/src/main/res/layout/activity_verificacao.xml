<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_verificacao">

    <!-- CARD CENTRAL -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="@drawable/bg_card"
        android:elevation="6dp"
        android:layout_margin="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- ÍCONE -->
        <ImageView
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@android:drawable/ic_lock_lock"
            app:tint="#2196F3"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- TÍTULO -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Verificação de Segurança"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#000"
            android:gravity="center"
            android:layout_marginBottom="8dp"/>

        <!-- SUBTÍTULO -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Digite o código de 6 dígitos"
            android:textSize="13sp"
            android:textColor="#666"
            android:gravity="center"
            android:layout_marginBottom="24dp"/>

        <!-- CAMPOS DE CÓDIGO RESPONSIVOS -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:weightSum="6"
            android:layout_marginBottom="24dp">

            <EditText android:id="@+id/code1" style="@style/code"/>
            <EditText android:id="@+id/code2" style="@style/code"/>
            <EditText android:id="@+id/code3" style="@style/code"/>
            <EditText android:id="@+id/code4" style="@style/code"/>
            <EditText android:id="@+id/code5" style="@style/code"/>
            <EditText android:id="@+id/code6" style="@style/code"/>

        </LinearLayout>

        <!-- BOTÃO CONFIRMAR -->
        <Button
            android:id="@+id/btnConfirmar"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:text="Confirmar código"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:backgroundTint="#2196F3"
            android:layout_marginBottom="12dp"/>

        <!-- REENVIAR CÓDIGO -->
        <TextView
            android:id="@+id/btnSms"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Reenviar código"
            android:textColor="#2196F3"
            android:textSize="13sp"
            android:gravity="center"
            android:padding="8dp"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>